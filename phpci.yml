build_settings:
    ignore:
        - "vendor"
        - "tests"
        - "app/cache"
        - "app/data"
        - "app/logs"
setup:
    composer:
        action: "update"

test:
    php_code_sniffer:
        standard: "PSR2"
